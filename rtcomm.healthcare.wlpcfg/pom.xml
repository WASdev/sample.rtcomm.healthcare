<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.wasdev.wlp.sample</groupId>
    <artifactId>sample.rtcomm.healthcare</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>rtcomm.healthcare.wlpcfg</artifactId>
  <name>RtcommHealthcare Server Assembly</name>
  <packaging>liberty-assembly</packaging>

  <dependencies>
    <dependency>
      <groupId>net.wasdev.wlp.sample</groupId>
      <artifactId>rtcomm.healthcare.app</artifactId>
      <version>1.0-SNAPSHOT</version>
      <type>war</type>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>net.wasdev.wlp.maven.plugins</groupId>
        <artifactId>liberty-maven-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <installDirectory>${wlp.install.dir}</installDirectory>
          <serverName>RtcommHealthcareServer</serverName>
          <outputDirectory>target</outputDirectory>
          <!-- Use custom server.xml -->
          <configFile>${project.basedir}/src/RtcommHealthcareServer/resources/wlp/server.xml</configFile>
        </configuration>

        <executions>
          <execution>
                      <id>deploy web application</id>
                      <phase>compile</phase>
                      <goals>
                          <goal>install-apps</goal>
                      </goals>
                      <configuration>
                          <appArtifact>
                              <groupId>net.wasdev.wlp.sample</groupId>
                              <artifactId>rtcomm.healthcare.app</artifactId>
                              <type>war</type>
                          </appArtifact>
                          <appsDirectory>apps</appsDirectory>
                          <stripVersion>true</stripVersion>
                      </configuration>
                  </execution>
        </executions>

      </plugin>
    </plugins>
  </build>

</project>
